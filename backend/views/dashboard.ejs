<%- include('layout-head', { title, activePage }) %>

<% const statusLabels = {
  redeemed: { label: 'Redeemed', color: 'bg-blue-100 text-blue-700', icon: '🎉' },
  accepted: { label: 'Accepted', color: 'bg-indigo-100 text-indigo-700', icon: '✅' },
  packed: { label: 'Packed', color: 'bg-purple-100 text-purple-700', icon: '📦' },
  in_transit: { label: 'In Transit', color: 'bg-cyan-100 text-cyan-700', icon: '🚚' },
  out_for_delivery: { label: 'Out for Delivery', color: 'bg-amber-100 text-amber-700', icon: '🛵' },
  delivered: { label: 'Delivered', color: 'bg-green-100 text-green-700', icon: '✅' },
}; %>

<!-- Status Cards -->
<div class="grid grid-cols-2 md:grid-cols-3 lg:grid-cols-6 gap-4 mb-8">
  <% STATUS_FLOW.forEach(function(s) { %>
    <div class="bg-white rounded-xl border border-gray-200 p-4 hover:shadow-md transition-shadow">
      <div class="flex items-center justify-between mb-2">
        <span class="text-2xl"><%= statusLabels[s].icon %></span>
        <span class="text-2xl font-bold text-gray-800"><%= counts[s] %></span>
      </div>
      <p class="text-xs font-medium text-gray-500"><%= statusLabels[s].label %></p>
    </div>
  <% }); %>
</div>

<!-- Summary Cards -->
<div class="grid grid-cols-1 md:grid-cols-2 gap-4 mb-8">
  <div class="bg-gradient-to-r from-orange-500 to-amber-500 rounded-xl p-6 text-white">
    <p class="text-sm opacity-90">Total Redemptions</p>
    <p class="text-3xl font-bold mt-1"><%= total %></p>
  </div>
  <div class="bg-gradient-to-r from-blue-500 to-indigo-500 rounded-xl p-6 text-white">
    <p class="text-sm opacity-90">Total Points Redeemed</p>
    <p class="text-3xl font-bold mt-1"><%= totalPoints.toLocaleString() %> ⚡</p>
  </div>
</div>

<!-- Recent Redemptions -->
<div class="bg-white rounded-xl border border-gray-200">
  <div class="px-6 py-4 border-b border-gray-200 flex items-center justify-between">
    <h3 class="font-semibold text-gray-800">Recent Redemptions</h3>
    <a href="/admin/redemptions" class="text-sm text-orange-600 hover:underline">View all</a>
  </div>
  <% if (recent.length === 0) { %>
    <div class="px-6 py-12 text-center text-gray-400">
      <p class="text-4xl mb-3">📭</p>
      <p>No redemptions yet. They will appear here when users redeem gifts.</p>
    </div>
  <% } else { %>
    <div class="overflow-x-auto">
      <table class="w-full">
        <thead>
          <tr class="text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
            <th class="px-6 py-3">Order ID</th>
            <th class="px-6 py-3">Gift</th>
            <th class="px-6 py-3">Category</th>
            <th class="px-6 py-3">Status</th>
            <th class="px-6 py-3">Date</th>
            <th class="px-6 py-3">Action</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-gray-100">
          <% recent.forEach(function(r) { %>
            <tr class="hover:bg-gray-50 transition-colors">
              <td class="px-6 py-3 text-sm font-mono text-gray-800"><%= r.order_id %></td>
              <td class="px-6 py-3 text-sm">
                <span class="mr-1"><%= r.gift_image %></span> <%= r.gift_name %>
              </td>
              <td class="px-6 py-3">
                <span class="text-xs px-2 py-1 rounded-full bg-gray-100 text-gray-600 capitalize"><%= r.gift_category %></span>
              </td>
              <td class="px-6 py-3">
                <span class="status-badge status-<%= r.status %>"><%= statusLabels[r.status].label %></span>
              </td>
              <td class="px-6 py-3 text-sm text-gray-500"><%= new Date(r.created_at).toLocaleDateString() %></td>
              <td class="px-6 py-3">
                <a href="/admin/redemptions/<%= r.id %>" class="text-sm text-orange-600 hover:underline">View</a>
              </td>
            </tr>
          <% }); %>
        </tbody>
      </table>
    </div>
  <% } %>
</div>

<%- include('layout-foot') %>
